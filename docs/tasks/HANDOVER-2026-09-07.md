# Backlog cleanup, 2026-09-07 — what needs you

Written at the end of an autonomous cleanup run. Everything below is either
**blocked on a decision only you can make**, or **done but not proven**. Each
row has a recommendation; you only have to agree or not.

---

## 1. Needs a decision before anyone builds

| # | Question | My recommendation |
| --- | --- | --- |
| **B276** | Self-service journal creation is **open to anyone** on fernscout.ch. `signup` reports `enabled` on `/api/health` right now. Is that intended? | **Decide, then act — this is the one I would not leave.** Anyone can create a journal on your instance today. Either turn `signup` off, or put an invite code / allowlist in front of it. Do not leave it open by accident. |
| **B709** | Should every day have to answer the money question before it is written, or only at publish time? Moving it to publish means an unpublished day is never asked. | Move `costs` to `when: "publish"`, leave `coordinates` as is. An agent wrote options into the ticket; the mechanism is a one-line change once you pick. |
| **B546** | Open-Meteo's free tier is **non-commercial** and this instance takes money. | Either write to them and record the answer, or self-host. The self-host path is config-only — `OPEN_METEO_*_URL` overrides already exist. Left untouched pending your call. |
| **B211** | The demo journal's photographs have nothing to do with their captions. | A taste decision. Left for you deliberately; an agent picking replacement photos is exactly the guessing this repo forbids. |
| **B593** | A fulfilment instance's job intake has no admission control. | Blocked — depends on B590, which nobody has built. Leave in backlog. |
| **B594** | Re-run the security scan on B07's payment gate. | Needs a session with the Workflow tool. Cannot be done from a normal checkout. |

---

## 2. Built, but inert until you do something external

| # | State | What you must do |
| --- | --- | --- |
| **B386** | Code merged and tested. **Deliberately does nothing yet.** Put back in `backlog/`, not closed, because a WhatsApp recipient still cannot unsubscribe today. | Per language: create a **new** Meta template version with a 4th body variable (never edit/delete — the name is reserved 30 days), get it approved, then set that locale to `{"name": "<new>", "manageLink": true}` in `site/config.json`. |
| **B659** | Off-site backup copy merged, live, and correctly reporting `secondary: unknown`. | Pick a provider (B2 or Hetzner, ~1GB), get an append-only key, set `RESTIC_REPOSITORY_SECONDARY` in `/etc/fernscout/env`, `restic init` once by hand, confirm `.backup.secondary.state == "ok"`. |
| **B651** | Code half done. | On the server: decide whether `/var/lib/fernscout/config.json.bak` and `config.json.bak-b325` are wanted — `chown fernscout:fernscout` or `rm` — then run the backup and check health. |
| **B520** | Shipped (the systemd unit was reinstalled this afternoon). | Restart with an upload deliberately in flight and confirm `journalctl` shows a clean stop, no `final-sigterm timed out`. |

---

## 3. The twelve ops tickets — nobody has driven the live site

**B101 B102 B103 B104 B105 B106 B107 B109 B110 B403 B437 B546.** These are
engagements, not diffs; their deliverable is findings. I did not do them, because
they need someone driving the running instance.

Two are **already contradicted by the live server** and should be re-read before
anyone works them:

- **B104** says signup is "on at the server and off in every journal". `/api/health`
  says `signup: enabled`. See B276 above — this may be a live security issue, not a
  documentation one.
- **B105** says the same about contacts. `/api/health` says `contacts: enabled`.

**Recommendation:** run `test-the-live-site` for the rest, but settle B276/B104
first — it is the only item on this page with a security edge.

---

## 4. Left unmerged — needs one action from you

**Branch `w4-helper-consent`** (B743, B735, B744, B734 — consent recorded per
provider, per scope; withdrawing photo consent no longer withdraws everything).
Built, `npm run verify` green, committed on its branch. **Not merged**, because
another session was mid-merge in the shared checkout with conflicts in
`components/travel/Vehicle.tsx` and `lib/photobook/travellers.ts` — files I know
nothing about, so I left their work alone.

```bash
# once that session's merge is finished and main is clean:
git merge --no-ff w4-helper-consent
npm run verify
```

There is also a `b688-signup` branch from another session, unmerged. Not mine.

---

## 5. Two things I closed that a person has not actually seen

You asked me to move `testing/` into `completed/`. I did — **440 tickets**, most
merged by other sessions over days, none verified by a human. That is what you
asked for and it is your call, but `completed/` no longer means "a person saw
this work". Worth knowing before you trust the lane.

**B373 and B377** I marked *superseded* on a **code reading**, not on a run. The
live server shows a backup failure at 01:35 today — that predates B653's deploy
at 08:25, so it is the old mechanism and does not disprove the fix. But **no
nightly has run under the new code yet.** Tonight's is the first. Both ticket
files carry this caveat and the reopen condition.

---

## 6. Worth your attention, though not blocked

- **B738** — under `--sequence.shuffle` the suite fails 7 then 27 different
  tests. There is a **real order-dependence problem** underneath the flake I
  fixed today (B713 was a genuine 0.34% coin-flip assertion; that one is gone
  and 5 consecutive clean runs confirm it). B738 is the bigger fish.
- **B736** — the demo generator still cannot reproduce the committed demo. An
  agent fixed four real bugs, then hit content it would have had to *invent* and
  stopped rather than ship guessed demo data. Correct call; the gaps are listed.
- **B750** — new: a consented provider is never checked against the configured
  one, so swapping providers does not re-ask.

---

## What actually landed

~60 tickets merged across 11 branches, in four waves, each verified in its own
worktree and again on `main`. Two deploys to fernscout.ch, both healthy, with the
changes checked against the live server rather than assumed.

Test suite: **4225 → 4544**. `npm run unused` now **fails** on a new unused
export instead of printing 153 of them.

Six tickets were **superseded rather than built** — already fixed by later work
(B243, B613, B428, B373, B377, B521) — and one was a duplicate (B65 → B659).
