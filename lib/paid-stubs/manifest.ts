// Public stub: which paid capabilities this build carries. None — see
// lib/capabilities.ts, which refuses to boot with any of them switched on.
// The real paid/manifest.ts lists the features paid/ provides.
export const PAID_AREAS: readonly string[] = [];
