/* eslint-disable @typescript-eslint/no-unused-vars -- a stub keeps the real signature and ignores its arguments */
// Public stub: photobooks are not included in this build — no entry point.
import type { PhotobookEntry, Trip } from "@/lib/types";

export async function photobookEntryFor(_trip: Trip): Promise<PhotobookEntry | undefined> {
  return undefined;
}
