/* eslint-disable @typescript-eslint/no-unused-vars -- a stub keeps the real signature and ignores its arguments */
// Public stub: postcards are not included in this build — no entry point.
import type { PostcardEntry, Trip } from "@/lib/types";

export async function postcardEntryFor(_trip: Trip): Promise<PostcardEntry | undefined> {
  return undefined;
}
