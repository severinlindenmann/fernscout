/* eslint-disable @typescript-eslint/no-unused-vars -- a stub keeps the real signature and ignores its arguments */
// Public stub: postcards are not included in this build.
import type { MediaTile } from "@/lib/types";

export default function PostcardSheet(_props: {
  username: string;
  trip: string;
  tile: MediaTile;
  from: string;
  onClose: () => void;
}): null {
  return null;
}
